# `zocai-evolution`

Parametric hardware evolution engine — Layer 5 of the Zoc AI Ecosystem.

Scaffolding skeleton (task 1.1). Trajectory capture, the shared-memory weight
bus, and the feature-flagged distillation pipeline (Requirement 12) are added
in later tasks. Replaces the legacy `python/llama_studio_neural` package once
the build-gated migration (Requirement 13) completes.
