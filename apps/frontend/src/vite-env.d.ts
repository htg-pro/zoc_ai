/// <reference types="vite/client" />

// Injected at build time by Vite `define` (see vite.config.ts). Holds the
// canonical app version sourced from the repo VERSION file.
declare const __APP_VERSION__: string;
